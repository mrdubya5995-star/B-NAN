{
  "name": "bnan-ios",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "copy-web": "node copy-web.js",
    "add-platform": "npm run copy-web && npx cap add ios",
    "sync": "npm run copy-web && npx cap sync ios",
    "open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.0",
    "@capacitor/ios": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}
